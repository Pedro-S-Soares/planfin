mutation CreatePeriod($name: String, $startDate: String!, $endDate: String!, $dailyLimit: String!, $totalBudget: String) {
  createPeriod(name: $name, startDate: $startDate, endDate: $endDate, dailyLimit: $dailyLimit, totalBudget: $totalBudget) {
    id
    name
    status
    dailyLimit
    totalBudget
    remainingTotal
  }
}

mutation UpdatePeriod($dailyLimit: String, $totalBudget: String) {
  updatePeriod(dailyLimit: $dailyLimit, totalBudget: $totalBudget) {
    id
    dailyLimit
    totalBudget
    remainingTotal
    status
  }
}

mutation CreateExpense($amount: String!, $date: String!, $note: String, $isExtra: Boolean, $subcategoryId: ID, $type: String) {
  createExpense(amount: $amount, date: $date, note: $note, isExtra: $isExtra, subcategoryId: $subcategoryId, type: $type) {
    id
    amount
    date
    note
    isExtra
    subcategory {
      id
      name
    }
    createdBy {
      id
      email
    }
  }
}

mutation UpdateExpense($id: ID!, $amount: String, $date: String, $note: String, $isExtra: Boolean, $subcategoryId: ID, $type: String) {
  updateExpense(id: $id, amount: $amount, date: $date, note: $note, isExtra: $isExtra, subcategoryId: $subcategoryId, type: $type) {
    id
    amount
    date
    note
    isExtra
    subcategory {
      id
      name
    }
  }
}

mutation DeleteExpense($id: ID!) {
  deleteExpense(id: $id)
}

mutation CreateCategory($name: String!, $type: String) {
  createCategory(name: $name, type: $type) {
    id
    name
    type
    subcategories {
      id
      name
    }
  }
}

mutation UpdateCategory($id: ID!, $name: String!, $type: String) {
  updateCategory(id: $id, name: $name, type: $type) {
    id
    name
    type
  }
}

mutation DeleteCategory($id: ID!) {
  deleteCategory(id: $id)
}

mutation CreateSubcategory($categoryId: ID!, $name: String!) {
  createSubcategory(categoryId: $categoryId, name: $name) {
    id
    name
  }
}

mutation DeleteSubcategory($id: ID!) {
  deleteSubcategory(id: $id)
}
